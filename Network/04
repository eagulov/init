# Identifiy the default gateway in the routing table

route get default | grep gateway | awk '{print $2}'

# Route to default
# Take row with 'gateway'
# 'awk' is used to print 2nd word which is gateway number
