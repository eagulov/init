# Use the output of the previous command to find the name and IP address of the
# device that makes the link between you (local network) and the outside world

nat (10.90.1.11)

# is the output from 'traceroute slash16.org'
# Network Address Translation (NAT) is process where network device, usually firewall,
# assigns public address to computer (or group of computers) inside private network.
# Main use of NAT is to limit number of public IP addresses organization or company must# use, for both economy and security purposes.
